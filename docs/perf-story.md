# Performance story — EXPLAIN PLAN side by side

> Generated by `python -m quantumsettle.bench.explain`. Re-run any time
> to refresh against the current schema.

The benchmark numbers in the README tell you *how much* faster the
optimized version is. The plans below tell you *why* — partition
pruning, global vs. local index choice, and materialized-view query
rewrite.

## What to read first

Each probe below has two plans. When comparing them, watch:

- **`Operation`** — `PARTITION RANGE SINGLE` means Oracle pruned to
  one partition. `PARTITION RANGE ALL` means it had to open every one.
- **`Pstart` / `Pstop`** — the partition ID range actually visited.
- **`Name`** — which index or MV was used.
- **`Cost`** — the optimizer's estimate; lower is better, but it is
  an *estimate*. Wall-clock numbers in `perf_metrics` are ground truth.

## Process UPDATE — partition pruning (Phase 2)

**What's happening:** Updating every unprocessed trade for one trade_date. The optimized version touches one partition; the not-optimized version scans every partition.

**Why it matters:** This is the headline win in the project. Both queries achieve the same result — set processed='Y', derive settlement_date and net_amount — but only the optimized one keeps trade_date in the WHERE clause. Because trade_date is the table's partition key, the planner prunes to a SINGLE partition. The not-optimized version locates each trade by trade_id alone and has to scan every partition.

### Optimized version

```sql
UPDATE trades
               SET processed = 'Y',
                   net_amount = CASE side
                                    WHEN 'BUY' THEN gross_amount + commission
                                    ELSE             gross_amount - commission
                                END,
                   updated_at = SYSTIMESTAMP
             WHERE trade_date = DATE '2026-05-12'
               AND processed  = 'N'
```

```
Plan hash value: 3219833098
 
-----------------------------------------------------------------------------------------------------------------------------------
| Id  | Operation                                   | Name                | Rows  | Bytes | Cost (%CPU)| Time     | Pstart| Pstop |
-----------------------------------------------------------------------------------------------------------------------------------
|   0 | UPDATE STATEMENT                            |                     |     1 |    96 |     2   (0)| 00:00:01 |       |       |
|   1 |  UPDATE                                     | TRADES              |       |       |            |          |       |       |
|   2 |   PARTITION RANGE SINGLE                    |                     |     1 |    96 |     2   (0)| 00:00:01 |    18 |    18 |
|   3 |    TABLE ACCESS BY LOCAL INDEX ROWID BATCHED| TRADES              |     1 |    96 |     2   (0)| 00:00:01 |    18 |    18 |
|*  4 |     INDEX RANGE SCAN                        | IX_TRADES_PROCESSED |     1 |       |     1   (0)| 00:00:01 |    18 |    18 |
-----------------------------------------------------------------------------------------------------------------------------------
 
Predicate Information (identified by operation id):
---------------------------------------------------
 
   4 - access("TRADE_DATE"=TO_DATE(' 2026-05-12 00:00:00', 'syyyy-mm-dd hh24:mi:ss') AND "PROCESSED"='N')
 
Note
-----
   - dynamic statistics used: dynamic sampling (level=2)
```

### Not optimized version

```sql
UPDATE trades
               SET processed = 'Y',
                   net_amount = CASE side
                                    WHEN 'BUY' THEN gross_amount + commission
                                    ELSE             gross_amount - commission
                                END,
                   updated_at = SYSTIMESTAMP
             WHERE trade_id = 12345
```

```
Plan hash value: 486041116
 
-----------------------------------------------------------------------------------------------
| Id  | Operation            | Name   | Rows  | Bytes | Cost (%CPU)| Time     | Pstart| Pstop |
-----------------------------------------------------------------------------------------------
|   0 | UPDATE STATEMENT     |        |    54 |  5886 |   274   (0)| 00:00:01 |       |       |
|   1 |  UPDATE              | TRADES |       |       |            |          |       |       |
|   2 |   PARTITION RANGE ALL|        |    54 |  5886 |   274   (0)| 00:00:01 |     1 |1048575|
|*  3 |    TABLE ACCESS FULL | TRADES |    54 |  5886 |   274   (0)| 00:00:01 |     1 |1048575|
-----------------------------------------------------------------------------------------------
 
Predicate Information (identified by operation id):
---------------------------------------------------
 
   3 - filter("TRADE_ID"=12345)
 
Note
-----
   - dynamic statistics used: dynamic sampling (level=2)
```

**What to look for:**

The `Pstart` / `Pstop` columns. Optimized: a single partition number (PARTITION RANGE SINGLE). Not optimized: `1` to `KEY` or `FINAL` (PARTITION RANGE ALL) — every partition is opened. With a row-by-row cursor loop, that cost is paid PER TRADE.

---

## Trade lookup by external reference (Phase 1)

**What's happening:** Finding one trade by its source-system reference. Used for idempotency checks during ingest.

**Why it matters:** The unique constraint on (source_system, external_trade_ref) is GLOBAL — not LOCAL — precisely so a single-row lookup does not have to scan every partition. The plan shows INDEX UNIQUE SCAN, which is O(log n).

### Optimized version

```sql
SELECT trade_id, trade_date, processed
              FROM trades
             WHERE source_system      = 'FRONT_OMS'
               AND external_trade_ref = 'EXT-20260512-000001234'
```

```
Plan hash value: 4175402690
 
---------------------------------------------------------------------------------------------------------------------------
| Id  | Operation                          | Name                 | Rows  | Bytes | Cost (%CPU)| Time     | Pstart| Pstop |
---------------------------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT                   |                      |     1 |    58 |     0   (0)| 00:00:01 |       |       |
|   1 |  TABLE ACCESS BY GLOBAL INDEX ROWID| TRADES               |     1 |    58 |     0   (0)| 00:00:01 | ROWID | ROWID |
|*  2 |   INDEX UNIQUE SCAN                | UK_TRADES_SOURCE_REF |     1 |       |     0   (0)| 00:00:01 |       |       |
---------------------------------------------------------------------------------------------------------------------------
 
Predicate Information (identified by operation id):
---------------------------------------------------
 
   2 - access("SOURCE_SYSTEM"='FRONT_OMS' AND "EXTERNAL_TRADE_REF"='EXT-20260512-000001234')
```

### Not optimized version

```sql
SELECT trade_id, trade_date, processed
              FROM trades
             WHERE external_trade_ref = 'EXT-20260512-000001234'
```

```
Plan hash value: 3376924971
 
------------------------------------------------------------------------------------------------------------------
| Id  | Operation                 | Name                 | Rows  | Bytes | Cost (%CPU)| Time     | Pstart| Pstop |
------------------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT          |                      |    54 |  2484 |   149   (0)| 00:00:01 |       |       |
|   1 |  VIEW                     | index$_join$_001     |    54 |  2484 |   149   (0)| 00:00:01 |       |       |
|*  2 |   HASH JOIN               |                      |       |       |            |          |       |       |
|*  3 |    HASH JOIN              |                      |       |       |            |          |       |       |
|*  4 |     HASH JOIN             |                      |       |       |            |          |       |       |
|   5 |      PARTITION RANGE ALL  |                      |    54 |  2484 |    28   (0)| 00:00:01 |     1 |1048575|
|   6 |       INDEX FAST FULL SCAN| IX_TRADES_INSTRUMENT |    54 |  2484 |    28   (0)| 00:00:01 |     1 |1048575|
|   7 |      PARTITION RANGE ALL  |                      |    54 |  2484 |    75   (0)| 00:00:01 |     1 |1048575|
|   8 |       INDEX FAST FULL SCAN| IX_TRADES_PROCESSED  |    54 |  2484 |    75   (0)| 00:00:01 |     1 |1048575|
|   9 |     PARTITION RANGE ALL   |                      |    54 |  2484 |    29   (0)| 00:00:01 |     1 |1048575|
|  10 |      INDEX FAST FULL SCAN | PK_TRADES            |    54 |  2484 |    29   (0)| 00:00:01 |     1 |1048575|
|* 11 |    INDEX FAST FULL SCAN   | UK_TRADES_SOURCE_REF |    54 |  2484 |     0   (0)| 00:00:01 |       |       |
------------------------------------------------------------------------------------------------------------------
 
Predicate Information (identified by operation id):
---------------------------------------------------
 
   2 - access(ROWID=ROWID)
   3 - access(ROWID=ROWID)
   4 - access(ROWID=ROWID)
  11 - filter("EXTERNAL_TRADE_REF"='EXT-20260512-000001234')
 
Note
-----
   - dynamic statistics used: dynamic sampling (level=2)
```

**What to look for:**

Optimized: INDEX UNIQUE SCAN on UK_TRADES_SOURCE_REF — one index probe. Not optimized (missing source_system so the unique index is unusable): a much wider scan with PARTITION RANGE ALL.

---

## Daily P&L aggregation — MV query rewrite (Phase 3)

**What's happening:** Aggregating trades into daily P&L per (account, currency). Optimized version reads the pre-aggregated MV; not optimized re-aggregates the base table.

**Why it matters:** MV_DAILY_PNL keeps the aggregation permanently up to date (REFRESH FAST ON COMMIT). The optimized query just reads a handful of pre-summarised rows. The not-optimized query does the full GROUP BY against the partition every time — and the NO_REWRITE hint stops the planner from quietly substituting the MV behind the scenes, so the cost is real.

### Optimized version

```sql
SELECT trade_date, account_id, currency,
                   SUM(trade_count) AS trades, SUM(sum_net_amount) AS net
              FROM mv_daily_pnl
             WHERE trade_date = DATE '2026-05-12'
             GROUP BY trade_date, account_id, currency
```

```
Plan hash value: 1772730317
 
--------------------------------------------------------------------------------------
| Id  | Operation             | Name         | Rows  | Bytes | Cost (%CPU)| Time     |
--------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT      |              |    40 |  2040 |     4  (25)| 00:00:01 |
|   1 |  HASH GROUP BY        |              |    40 |  2040 |     4  (25)| 00:00:01 |
|*  2 |   MAT_VIEW ACCESS FULL| MV_DAILY_PNL |    40 |  2040 |     3   (0)| 00:00:01 |
--------------------------------------------------------------------------------------
 
Predicate Information (identified by operation id):
---------------------------------------------------
 
   2 - filter("TRADE_DATE"=TO_DATE(' 2026-05-12 00:00:00', 'syyyy-mm-dd 
              hh24:mi:ss'))
 
Note
-----
   - dynamic statistics used: dynamic sampling (level=2)
```

### Not optimized version

```sql
SELECT /*+ NO_REWRITE */
                   trade_date, account_id, currency,
                   COUNT(*) AS trades, SUM(net_amount) AS net
              FROM trades
             WHERE trade_date = DATE '2026-05-12'
             GROUP BY trade_date, account_id, currency
```

```
Plan hash value: 2057938264
 
--------------------------------------------------------------------------------------------------
| Id  | Operation               | Name   | Rows  | Bytes | Cost (%CPU)| Time     | Pstart| Pstop |
--------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT        |        |  1725 | 65550 |   276   (1)| 00:00:01 |       |       |
|   1 |  HASH GROUP BY          |        |  1725 | 65550 |   276   (1)| 00:00:01 |       |       |
|   2 |   PARTITION RANGE SINGLE|        |  1725 | 65550 |   275   (1)| 00:00:01 |    18 |    18 |
|*  3 |    TABLE ACCESS FULL    | TRADES |  1725 | 65550 |   275   (1)| 00:00:01 |    18 |    18 |
--------------------------------------------------------------------------------------------------
 
Predicate Information (identified by operation id):
---------------------------------------------------
 
   3 - filter("TRADE_DATE"=TO_DATE(' 2026-05-12 00:00:00', 'syyyy-mm-dd hh24:mi:ss'))
 
Note
-----
   - dynamic statistics used: dynamic sampling (level=2)
```

**What to look for:**

Optimized: a small full scan of MV_DAILY_PNL (already aggregated, so very few rows). Not optimized: PARTITION RANGE SINGLE on TRADES plus a HASH GROUP BY — fast on small data, expensive on large data and on every single call.

---

## A note on AUTOTRACE-style runtime stats

Full AUTOTRACE (logical reads, physical reads, sorts, etc.) requires
SELECT on V$SQL_PLAN_STATISTICS_ALL and V$SQL, which only SYS can grant.
In container setups where you connect as SYSTEM, those grants fail.
The wall-clock numbers in `perf_metrics` are the practical alternative —
captured via DBMS_UTILITY.GET_TIME by `PKG_PERF.start_run` / `end_run`.